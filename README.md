# 📈 CurrencyBot — Telegram-бот для курсов валют

Telegram-бот, который показывает актуальные курсы валют к белорусскому рублю (BYN), позволяет подписываться на ежедневные уведомления в удобное время и с учётом часового пояса. Поддерживает гибкую настройку подписок, просмотр списка, выбор времени и часового пояса.

## 🚀 Возможности

- `/start` — приветствие и инструкция
- `/rate [валюта]` — курс выбранной валюты к BYN (по умолчанию USD)
- `/subscribe` — подписка на ежедневную рассылку (выбор валюты и времени)
- `/unsubscribe` — отмена подписки на выбранную валюту
- `/subscriptions` — список активных подписок
- `/set_timezone` — установка часового пояса (по геолокации или из списка)
- Хранение подписок пользователей в SQLite
- Уведомления приходят в выбранное время с учётом часового пояса пользователя
- Cron-рассылка с помощью `node-cron`
- Курсы берутся с официального API НБРБ

**Поддерживаемые валюты:** USD, EUR, RUB, CNY, PLN

---

## 🛠️ Стек

- Node.js + TypeScript
- [grammY](https://grammy.dev/) — Telegram Bot API
- Axios — запросы к API курсов валют
- SQLite + better-sqlite3 — простая БД
- node-cron — планировщик рассылки
- luxon — работа с датой/временем и часовыми поясами
- tz-lookup — определение часового пояса по координатам
- dotenv — переменные окружения
- ts-node-dev — запуск в разработке

---

## 📁 Структура проекта (FSD light)

```
src/
├── app/                  # Точка входа
├── bots/telegram/        # Telegram-бот
├── shared/api/           # API для курсов валют (НБРБ)
├── shared/db/            # Работа с SQLite и схема
├── entities/user/        # Репозиторий пользователей и подписок
├── features/rates/       # Команда /rate
├── features/subscribe/   # Подписка, отписка, список подписок
├── features/notifier/    # Механика уведомлений
├── features/timezone/    # Работа с часовыми поясами
```

---

## ⚙️ Установка

### Требования

- Node.js 18+ 
- npm или yarn
- Git

### Клонирование и установка

```bash
git clone https://github.com/your-username/kursik-bot.git
cd kursik-bot
npm install
```

Создай файл окружения с токеном:

```bash
cp .env.example .env
# Отредактируй .env и добавь токен бота
```

Или создай `.env` вручную:
```
BOT_TOKEN=your_telegram_bot_token
NODE_ENV=development  # или production
```

**Боты:**
- **Продакшн:** [@pandimun_kursik_bot](https://t.me/pandimun_kursik_bot) — основной бот для пользователей
- **Разработка:** [@pandimun_kursik_dev_bot](https://t.me/pandimun_kursik_dev_bot) — бот для тестирования новых функций

---

## 🔧 Переменные окружения

Проект использует единый файл `.env` для всех окружений:

- **`.env.example`** — базовый шаблон с плейсхолдерами
- **`.env`** — основной файл окружения (создается из .env.example)

**Доступные переменные:**
- `BOT_TOKEN` — токен Telegram-бота (обязательно)
- `NODE_ENV` — режим окружения (development/production)

**Пример использования:**
```bash
# Копируем базовый шаблон
cp .env.example .env

# Редактируем и добавляем токен
nano .env
```

## 🚴 Запуск в разработке

```bash
npm run dev
```

### Доступные скрипты

- `npm run dev` — запуск в режиме разработки с автоперезагрузкой
- `npm run build` — сборка TypeScript проекта
- `npm run start` — запуск собранного проекта
- `npm run copy-assets` — копирование SQL файлов в dist

---

## 🐳 Docker и контейнеризация

Проект поддерживает запуск через Docker для удобного развертывания.

### Быстрый запуск с Docker

```bash
# Клонируем репозиторий
git clone https://github.com/your-username/kursik-bot.git
cd kursik-bot

# Копируем файл окружения
cp env.example .env

# Редактируем .env и добавляем токен бота
nano .env

# Запускаем через Docker Compose
docker-compose up -d
```

### Управление контейнером

```bash
# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Обновление и перезапуск
docker-compose down && docker-compose up -d --build
```

### Автоматический деплой

Используйте готовые скрипты для деплоя:

**Linux/macOS:**
```bash
chmod +x deploy.sh
./deploy.sh
```

**Windows PowerShell:**
```powershell
.\deploy.ps1
```

---

Используется SQLite. При первом запуске автоматически создаётся файл `data.sqlite` и таблица `subscriptions` для хранения подписок пользователей:

```
chat_id INTEGER   # Telegram ID пользователя
currency TEXT     # Валюта (USD, EUR, ...)
hour INTEGER      # Час отправки уведомления (0-23)
timezone TEXT     # Часовой пояс (например, Europe/Minsk)
```

---

## ⏰ Уведомления и подписки

- Для каждой подписки можно выбрать валюту и время (09:00, 12:00, 18:00).
- Время отправки учитывает выбранный пользователем часовой пояс.
- Часовой пояс можно установить через команду `/set_timezone` (по геолокации или из популярных вариантов).
- Все подписки и настройки хранятся в базе данных.

---

## 🧩 Курсы валют

Используется официальный API Национального банка Республики Беларусь (НБРБ):

### API Endpoint

```
GET https://www.nbrb.by/api/exrates/rates/{currency}?parammode=2
```

### Поддерживаемые валюты

- **USD** — доллар США
- **EUR** — евро  
- **RUB** — российский рубль
- **CNY** — китайский юань
- **PLN** — польский злотый

### Пример запроса

Для получения курса USD:
```
GET https://www.nbrb.by/api/exrates/rates/USD?parammode=2
```

### Формат ответа

```json
{
  "Cur_ID": 145,
  "Date": "2024-01-15T00:00:00",
  "Cur_Abbreviation": "USD",
  "Cur_Scale": 1,
  "Cur_Name": "Доллар США",
  "Cur_OfficialRate": 3.25
}
```

### Особенности

- Курсы обновляются ежедневно в рабочие дни
- API возвращает официальный курс НБРБ
- При ошибке API бот показывает сообщение об ошибке
- Поддерживается кэширование для оптимизации запросов

---

## 🚀 Продакшн деплой

### PM2 (рекомендуется)

Для продакшн развертывания рекомендуется использовать PM2 для управления процессами:

```bash
# Установка PM2
npm install -g pm2

# Создание ecosystem файла
cat > ecosystem.config.js << EOF
module.exports = {
  apps: [{
    name: 'kursik-bot',
    script: 'dist/app/main.js',
    env: {
      NODE_ENV: 'production',
      BOT_TOKEN: process.env.BOT_TOKEN
    }
  }]
};
EOF

# Запуск через PM2
pm2 start ecosystem.config.js
pm2 save
pm2 startup
```

### Управление через PM2

```bash
# Просмотр статуса
pm2 list

# Просмотр логов
pm2 logs kursik-bot

# Перезапуск
pm2 restart kursik-bot

# Остановка
pm2 stop kursik-bot
```

### Обновление в продакшне

```bash
# Подтягиваем изменения
git pull origin main
npm install

# Перезапускаем через PM2
pm2 restart kursik-bot
```

**Подробная инструкция:** см. [PRODUCTION.md](PRODUCTION.md)

---

## 📊 Мониторинг и логирование

### Логирование

Бот выводит логи в консоль с информацией о:
- Запуске и остановке бота
- Ошибках при работе с API НБРБ
- Ошибках при работе с базой данных
- Статистике отправленных уведомлений

### Мониторинг через PM2

```bash
# Просмотр логов в реальном времени
pm2 logs kursik-bot --lines 100

# Мониторинг ресурсов
pm2 monit

# Статистика процессов
pm2 show kursik-bot
```

### Мониторинг через Docker

```bash
# Просмотр логов контейнера
docker-compose logs -f kursik-bot

# Статистика ресурсов
docker stats kursik-bot
```

---

## 🔮 Планы на будущее

- Поддержка нескольких базовых валют (не только BYN)
- Telegram Payments API для премиум-подписки
- Панель администратора для управления ботом
- Расширенная аналитика и статистика
- Поддержка криптовалют
- Интеграция с другими банками СНГ

---

## 👨‍💻 Разработка

### Архитектура проекта

Проект использует упрощенную версию Feature-Sliced Design (FSD):

- **`src/app/`** — точка входа приложения
- **`src/bots/telegram/`** — Telegram Bot API интеграция
- **`src/shared/`** — общие модули (API, БД)
- **`src/entities/`** — бизнес-сущности (пользователи)
- **`src/features/`** — функциональные модули (команды бота)

### Работа с кодом

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка проекта
npm run build

# Проверка типов TypeScript
npx tsc --noEmit
```

### Git workflow

Проект использует простой Git workflow с ветками:

- `main` — основная ветка для продакшна
- `feature/*` — новые функции
- `fix/*` — исправления багов
- `docs/*` — изменения документации

**Подробная инструкция:** см. [CONTRIBUTING.md](CONTRIBUTING.md)

---

## ❓ FAQ

### Как получить токен бота?

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в файл `.env`

### Бот не отвечает на команды

- Проверьте правильность токена в `.env`
- Убедитесь, что бот запущен (`pm2 list` или `docker-compose ps`)
- Проверьте логи на наличие ошибок

### Как изменить время уведомлений?

Используйте команду `/unsubscribe` для отмены текущей подписки, затем `/subscribe` для создания новой с нужным временем.

### Поддерживаются ли другие валюты?

В данный момент поддерживаются только USD, EUR, RUB, CNY, PLN. Добавление новых валют требует изменения кода.

---

## 📜 Лицензия

MIT License
